# Logo Changes

- Added `tabu_big_logo.webp` and `tabu_small_logo.webp` to the `public` directory.
- Updated `public/index.html` to include the logo image.
- Updated `views/Login.vue` to include special logo image for mobile screens.
- Added CSS media query to hide the big and small logo on mobile devices.